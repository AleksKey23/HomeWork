#include <stdio.h>
#include <iostream>

/*ссылка на репозиторий - https://github.com/AleksKey23/HomeWork.git */

main()
{	
 int x;
    int sum=0;
    
    for (x=1;x<100;x=x+1)
    {
        sum=sum+x;
        printf ("%i+", x);
    }
    
     sum=sum+x;
    printf ("%i = %i", x, sum);
 
 
}
